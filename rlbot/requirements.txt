# RLBot dependencies for playing trained models in actual Rocket League
# Install these ON WINDOWS where you have Rocket League installed

# Core dependencies - using flexible version requirements for compatibility
rlbot
stable-baselines3
torch
numpy
gymnasium

# CRITICAL: Use rlgym-compat for proper RLBot to RLGym observation conversion
# This ensures observations in gameplay match what was used during training
rlgym-compat
