# RLBot dependencies for playing trained models in actual Rocket League
# Install these ON WINDOWS where you have Rocket League installed

# RLBot framework
rlbot>=1.50.0

# These should already be installed from main project requirements
# but included here for completeness when setting up on Windows
stable-baselines3>=2.0.0
torch>=2.0.0
numpy>=1.24.0
gymnasium>=0.29.0
