# Core dependencies for RLGym with RocketSim
rocketsim>=1.0.0
git+https://github.com/AechPro/rocket-league-gym-sim@main#egg=rlgym-sim
stable-baselines3>=2.0.0
gymnasium>=0.28.0

# PyTorch - IMPORTANT: For GPU support, install CUDA version first
# CPU version (slower training):
#   torch>=2.0.0
# GPU version (CUDA 11.8 - recommended for training):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# GPU version (CUDA 12.1):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For other CUDA versions, visit: https://pytorch.org/get-started/locally/
torch>=2.0.0
numpy>=1.24.0,<2.0.0  # Pin to <2.0 for compatibility with gym (used by RLGym-Sim)
setuptools>=65.5.0,<70.0.0  # Avoid setuptools.extern.six issues

# Logging and monitoring
tensorboard>=2.13.0

# Utilities
pyyaml>=6.0
tqdm>=4.65.0
rich>=10.0.0  # Required for stable-baselines3 progress bar
